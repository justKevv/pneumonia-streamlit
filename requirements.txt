# Cloud-compatible (Linux) pins — do NOT use `uv pip freeze` output here,
# it locks macOS-only builds (e.g. tensorflow==2.21.0 has no cp314 wheel).
# Keep this minimal; pip resolves transitive deps on the deploy image.
# IMPORTANT: set Python 3.11 or 3.12 in the Streamlit Cloud dashboard
# (App settings -> Python version). TensorFlow has no Python 3.14 build.
streamlit>=1.32
tensorflow>=2.15
pillow
numpy
h5py
